# Flutter runtime environment (safe defaults only)
# Do NOT commit real secrets here for production.

# Backend service URL consumed by the app
ETA_SERVICE_URL=http://10.0.2.2:8081

# Optional client-side key for legacy route rendering (prefer backend /directions)
DIRECTIONS_API_KEY=

# Backend endpoint used for directions proxy
DIRECTIONS_BACKEND_URL=http://10.0.2.2:8081
